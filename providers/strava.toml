# Provider configuration for Strava (www.strava.com)
# Defines login flow, list page selectors, and detail page JS extraction

[provider]
name = "strava"
login_url = "https://www.strava.com/login"
login_success_patterns = ["/dashboard", "/athlete", "/onboarding", "strava.com/feed"]
login_failure_patterns = ["/login", "/session", "/oauth"]
login_email_selector = '#email, input[name="email"]'
login_password_selector = '#password, input[name="password"]'
login_button_selector = 'button[type="submit"], #login-button'
login_error_selector = '.alert-error, .alert-danger, [class*="error-message"]'
login_otp_selector = 'input[name="code"], input[type="tel"], input[autocomplete="one-time-code"]'
vision_page_analysis_prompt = "providers/strava/page_analysis.md"
profile_url = "https://www.strava.com/athlete"
profile_js_extract = '''
(function() {
    var data = {};
    var name = document.querySelector('.athlete-name, h1.text-title1');
    if (name) data.display_name = name.textContent.trim();
    var avatar = document.querySelector('.avatar-img, img.avatar');
    if (avatar) data.profile_picture_url = avatar.src;
    var loc = document.querySelector('.location');
    if (loc) {
        var parts = loc.textContent.trim().split(',');
        if (parts[0]) data.city = parts[0].trim();
        if (parts[1]) data.country = parts[1].trim();
    }
    return JSON.stringify(data);
})()
'''

[provider.login_oauth_buttons]
google = 'text:Sign In With Google'
apple = 'text:Sign In With Apple'

[list_page]
url = "https://www.strava.com/athlete/training"
row_selector = "tr.training-activity-row"
link_selector = 'a[data-field-name="name"]'
id_regex = '/\/activities\/(\d+)/'
vision_prompt = "providers/strava/list_page.md"
# Activities come from Strava's training-LOG interval feed (see the
# `graph_date_range` pagination below), not the /athlete/training table. Each
# month's response stashes a `#interval-rides` React feed into
# window.__dravrCaptures whose `preFetchedEntries` carry the granular sport
# `type` (TrailRun/Ride/Kayaking — not the list page's flattened "Ride") and the
# real UTC `startDate`. This extractor reads those captures: it pulls the
# jQuery("#interval-rides").html("...") payload, lets a detached element
# entity-unescape `data-react-props`, then maps each Activity entry to the
# {id,name,type,date,time,distance,elevation,pace} shape build_activity_from_js_item consumes.
js_extract = '''
(function() {
    var captures = window.__dravrCaptures || {};
    var out = [];
    var seen = {};

    // The `*_subtitle` stat value tells us what the paired `*` value is. Strava
    // localizes subtitles (fr OR en), so each field matches both spellings.
    function statValueBySubtitle(stats, subtitleRe) {
        // stats is a flat array of { key, value } pairs where each metric is a
        // value entry ("stat_one") followed by its subtitle ("stat_one_subtitle").
        // Match a subtitle by its localized text, then read the sibling value
        // entry whose key is the subtitle key minus the "_subtitle" suffix.
        if (!stats || !stats.length) return undefined;
        var byKey = {};
        for (var i = 0; i < stats.length; i++) {
            var s = stats[i];
            if (s && s.key != null) byKey[s.key] = (s.value != null ? s.value : "");
        }
        for (var j = 0; j < stats.length; j++) {
            var st = stats[j];
            if (!st || st.key == null) continue;
            var key = String(st.key);
            if (key.indexOf("_subtitle") < 0) continue;
            var subVal = st.value != null ? String(st.value) : "";
            if (!subtitleRe.test(subVal)) continue;
            var valueKey = key.slice(0, -("_subtitle".length));
            if (byKey.hasOwnProperty(valueKey)) return byKey[valueKey];
        }
        return undefined;
    }

    // Pull the FIRST jQuery("#interval-rides").html( STRING ) argument out of the
    // raw interval response and decode it from a JS string literal back to HTML.
    // The argument is a double-quoted JS string literal (internal quotes and
    // backslashes escaped). JSON.parse decodes the common escapes; a manual
    // unescape covers literals JSON rejects (e.g. \' single-quote escapes).
    function extractIntervalHtml(body) {
        if (!body) return null;
        // Match .html( "<escaped string literal>" ), allowing whitespace and the
        // jQuery alias ($ or jQuery). [\s\S] is used instead of . so the literal
        // can span newlines inside the captured group.
        var m = body.match(/(?:jQuery|\$)\(\s*["']#interval-rides["']\s*\)\s*\.html\(\s*"((?:[^"\\]|\\[\s\S])*)"/);
        if (!m) return null;
        var inner = m[1];
        var html = null;
        try {
            html = JSON.parse('"' + inner + '"');
        } catch (e1) {
            try {
                // Fallback for JS-literal-only escapes that JSON.parse rejects.
                html = inner
                    .replace(/\\'/g, "'")
                    .replace(/\\"/g, '"')
                    .replace(/\\n/g, "\n")
                    .replace(/\\r/g, "\r")
                    .replace(/\\t/g, "\t")
                    .replace(/\\\//g, "/")
                    .replace(/\\\\/g, "\\");
            } catch (e2) {
                html = null;
            }
        }
        return html;
    }

    // Read appContext.preFetchedEntries from the interval HTML by letting the
    // browser entity-unescape data-react-props for us (a detached div's
    // getAttribute returns the already-unescaped attribute value).
    function entriesFromHtml(html) {
        if (!html) return [];
        try {
            var div = document.createElement("div");
            div.innerHTML = html;
            var node = div.querySelector(".react-feed-component");
            if (!node) return [];
            var propsStr = node.getAttribute("data-react-props");
            if (!propsStr) return [];
            var props = JSON.parse(propsStr);
            var entries =
                props &&
                props.appContext &&
                props.appContext.preFetchedEntries;
            return Array.isArray(entries) ? entries : [];
        } catch (e) {
            return [];
        }
    }

    function pushEntry(entry) {
        try {
            if (!entry || entry.entity !== "Activity") return;
            var a = entry.activity;
            if (!a) return;
            var id = a.id != null ? String(a.id) : "";
            if (!id || seen[id]) return;

            var stats = a.stats;
            // Prefer the activity's canonical numeric distance (meters), the same
            // way `time` uses a.elapsedTime above. The formatted `stats` distance
            // is locale-shaped and, in some interval-feed months, arrives as a
            // bare number with no km/mi unit — which the Rust parser then keeps as
            // km-treated-as-metres (the 2024 "0.0X km" bug). The numeric field is
            // unit-unambiguous and immune to that. Fall back to the formatted stat
            // only when the numeric field is absent (no regression for any year).
            var distance = (typeof a.distance === "number")
                ? a.distance
                : statValueBySubtitle(stats, /distance/i);
            var pace = statValueBySubtitle(stats, /allure|pace/i);
            var elevation = statValueBySubtitle(stats, /dénivel|denivel|elevation/i);

            seen[id] = true;
            out.push({
                id: id,
                name: a.activityName != null ? a.activityName : "Untitled",
                type: a.type != null ? a.type : "",
                date: a.startDate != null ? a.startDate : "",
                // elapsedTime is a numeric seconds value; pass it through as-is so
                // the Rust layer reads it via json_field_f64, not a display string.
                time: a.elapsedTime != null ? a.elapsedTime : 0,
                distance: distance != null ? distance : "",
                elevation: elevation != null ? elevation : "",
                suffer_score: a.sufferScore != null ? a.sufferScore : undefined,
                pace: pace != null ? pace : ""
            });
        } catch (e) {}
    }

    for (var k in captures) {
        if (!/graph_date_range/.test(k)) continue;
        try {
            var entry = captures[k];
            if (!entry || (entry.status != null && entry.status !== 200)) continue;
            var html = extractIntervalHtml(entry.body);
            if (!html) continue;
            var entries = entriesFromHtml(html);
            for (var i = 0; i < entries.length; i++) {
                pushEntry(entries[i]);
            }
        } catch (e) {}
    }

    // Fresh-head merge: the interval feed above only carries complete weeks,
    // so the newest days exist solely in the live-list capture stashed by the
    // pagination fresh_url_template fetch (training_activities {models:[...]}).
    // Interval entries win on id collision (granular type + the exact field
    // semantics the parser expects); models only fill the missing head.
    // start_time is "YYYY-MM-DDTHH:MM:SS+0000"; normalize to Z-suffixed
    // RFC3339 for the Rust parser. start_date_local_raw is a tz-shifted LOCAL
    // epoch (last resort); start_date is the date-only display string (worst).
    function startIso(m) {
        if (m.start_time) {
            var d = new Date(m.start_time);
            if (!isNaN(d.getTime())) return d.toISOString().replace(/\.\d+Z$/, "Z");
        }
        if (m.start_date_local_raw) {
            var dl = new Date(Number(m.start_date_local_raw) * 1000);
            if (!isNaN(dl.getTime())) return dl.toISOString().replace(/\.\d+Z$/, "Z");
        }
        return m.start_date || "";
    }

    for (var fk in captures) {
        if (!/\/athlete\/training_activities/.test(fk)) continue;
        try {
            var fentry = captures[fk];
            if (!fentry || (fentry.status != null && fentry.status !== 200)) continue;
            var parsed = JSON.parse(fentry.body);
            var arr = (parsed && parsed.models) || parsed;
            if (!Array.isArray(arr)) continue;
            for (var mi = 0; mi < arr.length; mi++) {
                var m = arr[mi];
                if (!m) continue;
                var mid = m.id != null ? String(m.id) : "";
                if (!mid || seen[mid]) continue;
                seen[mid] = true;
                out.push({
                    id: mid,
                    name: m.name || "Untitled",
                    type: m.sport_type || m.display_type || "",
                    date: startIso(m),
                    // elapsed_time_raw matches the interval entries' elapsedTime
                    // semantics so one merged list reports one time meaning;
                    // moving_time_raw only when elapsed is absent.
                    time: (m.elapsed_time_raw != null ? m.elapsed_time_raw : m.moving_time_raw),
                    distance: (m.distance_raw != null ? m.distance_raw : ""),
                    elevation: (m.elevation_gain_raw != null ? m.elevation_gain_raw : ""),
                    suffer_score: (m.suffer_score != null ? m.suffer_score : undefined)
                });
            }
        } catch (e) {}
    }

    return JSON.stringify(out);
})()
'''

[list_page.pagination]
# Walk Strava's training LOG a calendar month at a time via the same-origin
# `graph_date_range` XHR. The `{interval}` placeholder (presence => interval mode
# in the scraper) is `YYYYMM`; `{year_offset}` is how many years back that month
# sits from now; `{athlete_id}` is the logged-in numeric id resolved at scrape
# time. Each response carries that month's activities in a `#interval-rides`
# React feed (granular sport `type` + real UTC `startDate`), so a deep `after`
# (e.g. "my 2022 races") is reached in ~12 fetches a year instead of ~100
# `&page=N` list pages — fast enough to finish before a serverless instance is
# reclaimed. The js_extract above reads these captures.
url_template = "https://www.strava.com/athletes/{athlete_id}/graph_date_range?chart_type=miles&interval_type=month&interval={interval}&year_offset={year_offset}"
# Fresh-head top-up fetched once before the interval walk: graph_date_range
# only carries complete weeks, so without this the newest days (current week)
# never appear no matter how recent the start month. Page 1 of the live list
# XHR (~20 rows) covers the gap; the js_extract merges it deduped by id.
fresh_url_template = "https://www.strava.com/athlete/training_activities?keywords=&sport_type=&tags=&commute=&private_activities=&trainer=&gear=&new_activity_only=false&page={page}"

[list_page.fields]
name = 'a[data-field-name="name"]'
sport_type = 'td[data-field-name="sport_type"]'
date = "td.col-date"
time = 'td[data-field-name="time"]'
distance = "td.col-dist"
elevation = "td.col-elev"
suffer_score = "td.col-suffer-score"

[detail_page]
url_template = "https://www.strava.com/activities/{id}"
js_extract = '''
(function() {
    const data = {};

    // Activity name from h2 or second heading
    const headings = document.querySelectorAll('h1, h2');
    headings.forEach(function(h) {
        const text = h.textContent.trim();
        if (text.includes('\u2013') || text.includes('-')) {
            // h1 format: "Athlete Name \u2013 Trail Run" or "Athlete Name - Trail Run"
            const sep = text.includes('\u2013') ? '\u2013' : '-';
            data.type = text.split(sep).pop().trim();
        } else if (!data.name && !text.includes('Recent') && !text.includes('Strava')) {
            data.name = text;
        }
    });

    // Primary inline stats: Distance, Moving Time, Pace, Relative Effort, Speed, HR, Power, Cadence
    document.querySelectorAll('ul.inline-stats li').forEach(function(li) {
        const text = li.textContent.trim();
        if (text.includes('Distance')) {
            data.distance = text.replace('Distance', '').trim();
        } else if (text.includes('Moving Time') || text.includes('de d\u00e9placement')) {
            data.moving_time = text.replace(/Moving Time|Dur\u00e9e de d\u00e9placement/i, '').trim();
        } else if (text.includes('Pace') || text.includes('Allure')) {
            data.pace = text.replace(/Pace|Allure/i, '').trim();
        } else if (text.includes('Relative Effort') || text.includes('Effort relat')) {
            data.relative_effort = text.replace(/Relative Effort|Effort relatif/i, '').trim();
        } else if (text.includes('Speed') || text.includes('Vitesse')) {
            data.avg_speed = text.replace(/Speed|Vitesse/i, '').trim();
        } else if (text.includes('Avg HR') || text.includes('FC moy')) {
            data.avg_hr = text.replace(/Avg HR|FC moy\.?/i, '').trim();
        } else if (text.includes('Max HR') || text.includes('FC max')) {
            data.max_hr = text.replace(/Max HR|FC max\.?/i, '').trim();
        } else if (text.includes('Power') || text.includes('Puissance')) {
            data.avg_power = text.replace(/Power|Puissance/i, '').trim();
        } else if (text.includes('Cadence')) {
            data.cadence = text.replace('Cadence', '').trim();
        }
    });

    // Secondary stats: Elevation, Calories, Elapsed Time
    const moreStats = document.querySelector('div.more-stats');
    if (moreStats) {
        const text = moreStats.textContent;
        const elevMatch = text.match(/(?:Elevation|D\u00e9nivel\u00e9 positif)\s*([\d,.]+)\s*m/i);
        if (elevMatch) data.elevation = elevMatch[1];
        const calMatch = text.match(/(?:Calories|Calories)\s*([\d,.]+)/i);
        if (calMatch) data.calories = calMatch[1];
        const elapsedMatch = text.match(/(?:Elapsed Time|Temps \u00e9coul\u00e9)\s*([\d:]+)/i);
        if (elapsedMatch) data.elapsed_time = elapsedMatch[1];
    }

    // Weather stats (English and French)
    document.querySelectorAll('div.weather-stat').forEach(function(stat) {
        const text = stat.textContent.trim();
        if (/^(Clear|Cloudy|Overcast|Rain|Snow|D\u00e9gag|Nuageux|Couvert|Pluie|Neige)/i.test(text) && !text.includes('\u00b0')) {
            data.weather = text;
        } else if (/Temp\u00e9rature|Temperature/i.test(text)) {
            data.temperature = text.replace(/Temp\u00e9rature|Temperature/i, '').trim();
        } else if (/Humidit\u00e9|Humidity/i.test(text)) {
            data.humidity = text.replace(/Humidit\u00e9|Humidity/i, '').trim();
        } else if (/Ressenti|Feels like/i.test(text)) {
            data.feels_like = text.replace(/Ressenti|Feels like/i, '').trim();
        } else if (/Vitesse du vent|Wind Speed/i.test(text)) {
            data.wind_speed = text.replace(/Vitesse du vent|Wind Speed/i, '').trim();
        } else if (/Direction du vent|Wind Direction/i.test(text)) {
            data.wind_direction = text.replace(/Direction du vent|Wind Direction/i, '').trim();
        }
    });

    // Location from the date/location line. The <time> element here is
    // localized, zone-less text ("08:36, le vendredi 29 mai 2026") with no
    // `datetime` attribute — it is NOT a reliable start timestamp. The real UTC
    // start is read from the embedded activity JSON further below.
    const timeEl = document.querySelector('time');
    if (timeEl) {
        const container = timeEl.closest('div') || timeEl.parentElement;
        if (container) {
            const fullText = container.textContent.trim();
            const dashIdx = fullText.lastIndexOf(' - ');
            if (dashIdx > 0) {
                data.location = fullText.substring(dashIdx + 3).trim();
            }
        }
    }

    // Device name
    const allText = document.body.textContent;
    const devMatch = allText.match(/(?:Garmin|Apple Watch|Wahoo|Polar|Suunto|COROS|Fitbit|Samsung)[^\n,]*/i);
    if (devMatch) data.device = devMatch[0].trim();

    // Gear/shoes — extract from the gear section (div.gear span.gear-name)
    var gearName = document.querySelector('.gear .gear-name, .gear-name');
    if (gearName) {
        data.gear = gearName.textContent.trim().replace(/\s+/g, ' ');
    }

    // Perceived exertion
    const peMatch = allText.match(/(?:Perceived Exertion|Effort ressenti)\s*:?\s*([\w\s\u00e9]+?)(?:\n|$)/i);
    if (peMatch) data.perceived_exertion = peMatch[1].trim();

    // Try to extract structured data from the embedded activity JSON
    // Strava embeds a rich JSON blob via .similarActivitiesData() or pageView data
    var scripts = document.querySelectorAll('script');
    for (var s = 0; s < scripts.length; s++) {
        var src = scripts[s].textContent || '';
        var jsonMatch = src.match(/activity_values.*?"values"\s*:\s*(\{[^}]+\})/);
        if (jsonMatch) {
            try {
                var vals = JSON.parse(jsonMatch[1]);
                if (!data.avg_hr && vals.avg_hr) data.avg_hr = String(vals.avg_hr);
                if (!data.max_hr && vals.max_hr) data.max_hr = String(vals.max_hr);
                if (!data.cadence && vals.avg_cadence) data.cadence = String(vals.avg_cadence);
                if (!data.max_cadence) data.max_cadence = String(vals.max_cadence || '');
                if (!data.calories && vals.calories) data.calories = String(vals.calories);
                if (!data.elevation && vals.elev_gain) data.elevation = String(vals.elev_gain);
                if (vals.max_speed) data.max_speed = String(vals.max_speed);
                if (vals.avg_speed) data.avg_speed_ms = String(vals.avg_speed);
                if (vals.elapsed_time) data.elapsed_time_secs = String(vals.elapsed_time);
                if (vals.total_steps) data.total_steps = String(vals.total_steps);
                if (vals.grade_adjusted_distance) data.gap_distance = String(vals.grade_adjusted_distance);
            } catch(e) {}
            break;
        }
    }

    // GPS coordinates — Strava embeds the start lat/lng in several places.
    // Tries (in order): pageView.activity() global, "start_latlng":[lat,lng]
    // pattern in any <script>, then the Mapbox static-map URL on the activity
    // map image (`...,/<lng>,<lat>,zoom...`). Skips [0,0] which Strava emits
    // for indoor activities.
    function isPlausibleLatLng(lat, lng) {
        if (typeof lat !== 'number' || typeof lng !== 'number') return false;
        if (!isFinite(lat) || !isFinite(lng)) return false;
        if (Math.abs(lat) < 0.0001 && Math.abs(lng) < 0.0001) return false;
        return Math.abs(lat) <= 90 && Math.abs(lng) <= 180;
    }
    try {
        if (typeof pageView !== 'undefined' && pageView && typeof pageView.activity === 'function') {
            var act = pageView.activity();
            var attrs = (act && (act.attributes || (typeof act.toJSON === 'function' ? act.toJSON() : null))) || act;
            if (attrs && Array.isArray(attrs.start_latlng) && attrs.start_latlng.length >= 2) {
                var lat = Number(attrs.start_latlng[0]);
                var lng = Number(attrs.start_latlng[1]);
                if (isPlausibleLatLng(lat, lng)) {
                    data.start_latitude = String(lat);
                    data.start_longitude = String(lng);
                }
            }
        }
    } catch(e) {}
    // Real activity START timestamp (UTC). The visible <time> is localized and
    // zone-less, and quoted "start_date":"...ISO..." strings on this page are
    // SEGMENT EFFORTS (mid-ride), NOT the activity start — matching one of those
    // reports a segment split time as the start. Read the activity's own UTC
    // start_date *epoch* from the embedded JSON (e.g. similarActivitiesData),
    // anchored to this activity's id so we never pick a neighbour's or a
    // segment's value. Strava's startDateLocal is local-wall-time-as-epoch and
    // is intentionally NOT used (it would misattribute by the tz offset).
    if (!data.date || data.date.indexOf('T') < 0) {
        var actId = '';
        try {
            var aId = pageView.activity();
            actId = String((aId && (aId.id || (aId.attributes && aId.attributes.id))) || '');
        } catch (e) {}
        if (!actId) {
            var um = location.pathname.match(/activities\/(\d+)/);
            if (um) actId = um[1];
        }
        function epochToIso(sec) {
            var n = Number(sec);
            if (!isFinite(n) || n < 1000000000) return null;
            return new Date(n * 1000).toISOString().replace(/\.\d+Z$/, 'Z');
        }
        var pageHtml = document.documentElement.innerHTML;
        if (actId) {
            var reAnchored = new RegExp('"activity_id"\\s*:\\s*' + actId + '[\\s\\S]{0,400}?"start_date"\\s*:\\s*(\\d{9,11})');
            var mAnchored = pageHtml.match(reAnchored);
            if (mAnchored) {
                var isoA = epochToIso(mAnchored[1]);
                if (isoA) data.date = isoA;
            }
        }
        // Fallback: first UNQUOTED numeric start_date epoch. The quoted ISO
        // start_dates (segment efforts) cannot match this pattern.
        if (!data.date || data.date.indexOf('T') < 0) {
            var mFirst = pageHtml.match(/"start_date"\s*:\s*(\d{9,11})\b/);
            if (mFirst) {
                var isoF = epochToIso(mFirst[1]);
                if (isoF) data.date = isoF;
            }
        }
        // No fabrication: if still unset, the page exposed no activity start
        // epoch — leave data.date empty so the Rust layer fails visibly rather
        // than stamping the scrape moment (Utc::now).
    }
    if (!data.start_latitude) {
        for (var gs = 0; gs < scripts.length; gs++) {
            var gbody = scripts[gs].textContent || '';
            var llMatch = gbody.match(/"start_latlng"\s*:\s*\[\s*(-?\d+\.?\d*)\s*,\s*(-?\d+\.?\d*)\s*\]/);
            if (llMatch) {
                var glat = Number(llMatch[1]);
                var glng = Number(llMatch[2]);
                if (isPlausibleLatLng(glat, glng)) {
                    data.start_latitude = String(glat);
                    data.start_longitude = String(glng);
                    break;
                }
            }
        }
    }
    if (!data.start_latitude) {
        // Mapbox static map URL: …/static/<style>/<lng>,<lat>,<zoom>/…
        var mapImgs = document.querySelectorAll('img[src*="api.mapbox.com/styles"]');
        for (var mi = 0; mi < mapImgs.length; mi++) {
            var msrc = mapImgs[mi].getAttribute('src') || '';
            var mboxMatch = msrc.match(/\/static\/[^\/]+\/(-?\d+\.\d+),(-?\d+\.\d+),/);
            if (mboxMatch) {
                var mlng = Number(mboxMatch[1]);
                var mlat = Number(mboxMatch[2]);
                if (isPlausibleLatLng(mlat, mlng)) {
                    data.start_latitude = String(mlat);
                    data.start_longitude = String(mlng);
                    break;
                }
            }
        }
    }

    // Splits + laps — Strava embeds these as JSON arrays inside a page
    // bootstrap block keyed by "splits_metric" or "laps". Scripts can
    // carry the arrays in several shapes across page variants; walk
    // every <script> tag and extract the first occurrence of each key
    // that parses cleanly. Arrays at top level, arrays wrapped in
    // "splits_metric":[…] strings, and arrays embedded in a larger
    // JSON object are all handled. Parse failures on any one script
    // are swallowed — the next script may contain the data.
    function extractJsonArrayForKey(haystack, key) {
        var needle = '"' + key + '"';
        var k = haystack.indexOf(needle);
        if (k < 0) return null;
        var bracket = haystack.indexOf('[', k);
        if (bracket < 0) return null;
        var depth = 0;
        var inString = false;
        var escaped = false;
        for (var i = bracket; i < haystack.length; i++) {
            var ch = haystack[i];
            if (escaped) { escaped = false; continue; }
            if (ch === '\\\\') { escaped = true; continue; }
            if (ch === '"') { inString = !inString; continue; }
            if (inString) continue;
            if (ch === '[') depth++;
            else if (ch === ']') { depth--; if (depth === 0) {
                try { return JSON.parse(haystack.substring(bracket, i + 1)); }
                catch (e) { return null; }
            } }
        }
        return null;
    }
    for (var si = 0; si < scripts.length; si++) {
        var body = scripts[si].textContent || '';
        if (!data.splits) {
            var sp = extractJsonArrayForKey(body, 'splits_metric');
            if (Array.isArray(sp) && sp.length > 0) data.splits = sp;
        }
        if (!data.laps) {
            var lp = extractJsonArrayForKey(body, 'laps');
            if (Array.isArray(lp) && lp.length > 0) data.laps = lp;
        }
        if (data.splits && data.laps) break;
    }

    // Fallback: search page text for HR/cadence if not found in JSON
    if (!data.avg_hr) {
        var hrMatch = allText.match(/(\d+)\s*bpm/i);
        if (hrMatch) data.avg_hr = hrMatch[1];
    }
    if (!data.cadence) {
        var cadMatch = allText.match(/(\d+)\s*(?:ppm|rpm|spm)/i);
        if (cadMatch) data.cadence = cadMatch[1];
    }

    // GAP / VAP
    var gapMatch = allText.match(/(?:VAP|GAP)\s+([\d:]+\s*\/km)/i);
    if (gapMatch) data.gap = gapMatch[1].trim();

    return JSON.stringify(data);
})()
'''
vision_prompt = "providers/strava/detail_page.md"

# Fitness & Freshness page — shows current fitness, fatigue, and form scores
# No date parameter — always shows current values
[health_pages.fitness]
url_template = "https://www.strava.com/athlete/fitness"
js_extract = '''
(function() {
    var data = {};
    var stats = document.querySelectorAll('div.stat');
    stats.forEach(function(stat) {
        var label = stat.querySelector('p.data-label');
        var value = stat.querySelector('div.stat-text.current-fitness-values');
        if (!label || !value) return;
        var key = label.textContent.trim().toLowerCase();
        var val = value.textContent.trim();
        if (key === 'fitness') data.fitness = val;
        else if (key === 'fatigue') data.fatigue = val;
        else if (key === 'form') data.form = val;
    });

    // FTP from the inline form on the fitness page
    var ftpInput = document.querySelector('input[name="ftp"], input[id*="ftp"]');
    if (ftpInput && ftpInput.value) data.ftp = ftpInput.value;

    // Weight from the page (may be embedded in JS data)
    var weightInput = document.querySelector('input[name="weight"], input[id*="weight"]');
    if (weightInput && weightInput.value) data.weight_kg = weightInput.value;

    return JSON.stringify(data);
})()
'''
